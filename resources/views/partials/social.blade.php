<section>

  <div class="container">
      <button>SIGN-UP NOW!</button>
      <div>
        <span>FOLLOW US</span>
        {{-- <img
        v-for="(card, index) in productSocial" 
        :key="`ciclo-social${index}`"
        :src="card.img" alt=""
        > --}}
        
      </div>
    </div>
</section>